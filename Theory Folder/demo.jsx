// JSX - JavaScript XML

// JSX is a syntax extension for JavaScript that looks similar to HTML.

// JSX is used in React to describe the UI structure.

var h1 = <h1>Hello, world!</h1>;

function abc() {
	console.log("This is a demo function.");
}

<abc />;

// JSX allows you to write HTML-like code within JavaScript, making it easier to visualize the structure of the UI.

function Hero() {
	console.log("This is Hero component.");
}
<Hero />;

// JSX elements can have attributes, just like HTML elements.
